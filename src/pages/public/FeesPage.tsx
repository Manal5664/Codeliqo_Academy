import { FeeCard } from '../../components/FeeCard';
import { CTASection, PageHeader } from '../../components/ui';
import { programs } from '../../data/programs';
import { usePageMeta } from '../../hooks/usePageMeta';
export function FeesPage(){usePageMeta('Program Fees','Compare exact registration, monthly tuition and total costs for all six Codeliqo Academy programs.');return <><PageHeader eyebrow="Transparent fees" title="Compare program costs" description="Every program uses a PKR 1,000 registration fee plus the listed monthly tuition installments. There are no discounts or payment-processing options represented on this page."/><section className="section-pad"><div className="container-shell"><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{programs.map(program=><FeeCard key={program.slug} program={program}/>)}</div><div className="mt-8 rounded-2xl border border-slate-200 bg-slate-50 p-6 text-sm leading-6 text-slate-600"><strong className="text-navy-900">Payment information:</strong> Applicants receive payment instructions privately after application review. The student portal records verified payments and balances; it does not process online payments.</div></div></section><CTASection title="Choose your pathway before applying" copy="Review each program’s curriculum, prerequisites and expected workload alongside its fee structure."/></>}
