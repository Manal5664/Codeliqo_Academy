import { FeeCard } from '../../components/FeeCard';
import { CTASection, EmptyState, ErrorState, LoadingState, PageHeader } from '../../components/ui';
import { useProgramCatalog } from '../../features/programs/ProgramCatalogContext';
import { usePageMeta } from '../../hooks/usePageMeta';
export function FeesPage(){const {programs,loading,error,reload}=useProgramCatalog();usePageMeta('Program Fees','Compare Codeliqo Academy program costs.');return <><PageHeader eyebrow="Transparent fees" title="Compare program costs" description="Every program lists its complete cost. Existing installment plans are shown where available. There are no discounts or payment-processing options represented on this page."/><section className="section-pad"><div className="container-shell">{loading?<LoadingState label="Loading program fees"/>:error?<ErrorState message={error} onRetry={reload}/>:programs.length?<div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{programs.map(program=><FeeCard key={program.slug} program={program}/>)}</div>:<EmptyState title="No program fees are published yet" message="Published programs will appear here automatically."/>}<div className="mt-8 rounded-2xl border border-slate-200 bg-slate-50 p-6 text-sm leading-6 text-slate-600"><strong className="text-navy-900">Payment information:</strong> Applicants receive payment instructions privately after application review. The student portal records verified payments and balances; it does not process online payments.</div></div></section><CTASection title="Choose your pathway before applying" copy="Review each program’s curriculum, prerequisites and expected workload alongside its fee structure."/></>}
