import { LogOut, Menu, UserRound } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../features/auth/AuthContext';
import { loginPath } from '../features/auth/authPolicy';

export function DashboardHeader({title,onMenu}:{title:string;onMenu:()=>void}){const {profile,user,role,signOut}=useAuth();const navigate=useNavigate();const logout=async()=>{const destination=loginPath(role??'student');await signOut();navigate(destination,{replace:true});};return <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-slate-200 bg-white/95 px-5 backdrop-blur sm:px-8"><div className="flex items-center gap-3"><button onClick={onMenu} className="rounded-lg p-2 text-slate-600 lg:hidden" aria-label="Open navigation"><Menu/></button><div><p className="text-xs font-semibold text-slate-400">Codeliqo Academy</p><h1 className="text-lg font-bold sm:text-xl">{title}</h1></div></div><div className="flex items-center gap-2"><div className="hidden text-right sm:block"><p className="text-sm font-semibold text-navy-900">{profile?.full_name||'Academy user'}</p><p className="text-xs text-slate-500">{user?.email}</p></div><span className="grid size-10 place-items-center rounded-xl bg-slate-100 text-slate-600"><UserRound size={19}/></span><button onClick={()=>void logout()} className="rounded-xl p-2.5 text-slate-500 hover:bg-red-50 hover:text-red-600" aria-label="Sign out"><LogOut size={19}/></button></div></header>}
