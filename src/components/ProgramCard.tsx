import { ArrowRight, Clock3, Radio } from 'lucide-react';
import { Link } from 'react-router-dom';
import type { Program } from '../types';
import { categoryLabels } from '../data/programs';
import { formatPkr } from '../lib/utils';

export function ProgramCard({ program }: { program: Program }) { return <article className="card group flex h-full flex-col overflow-hidden p-6 transition duration-300 hover:-translate-y-1 hover:border-brand-200 hover:shadow-glow"><div className="mb-5 flex items-start justify-between gap-3"><span className="badge bg-brand-50 text-brand-700">{categoryLabels[program.category]}</span><span className="text-xs font-bold text-slate-400">{program.code}</span></div><h3 className="text-xl font-bold leading-snug group-hover:text-brand-700">{program.shortTitle}</h3><p className="mt-3 flex-1 text-sm leading-6 text-slate-600">{program.description}</p><div className="mt-5 grid grid-cols-2 gap-3 border-y border-slate-100 py-4 text-sm"><span className="flex items-center gap-2 text-slate-600"><Clock3 size={15} className="text-brand-600" />{program.duration.split(' / ')[0]}</span><span className="flex items-center gap-2 text-slate-600"><Radio size={15} className="text-brand-600" />{program.liveHours} live hrs</span></div><div className="mt-5 flex items-end justify-between gap-3"><div><p className="text-xs text-slate-500">Total program cost</p><p className="font-bold text-navy-900">{formatPkr(program.total)}</p></div><Link to={`/programs/${program.slug}`} className="inline-flex items-center gap-1 text-sm font-semibold text-brand-700">View program<ArrowRight size={15} /></Link></div></article>; }
