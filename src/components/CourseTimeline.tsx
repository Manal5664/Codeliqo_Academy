import { ChevronDown } from 'lucide-react';
import type { CurriculumModule } from '../types';

export function CourseTimeline({ modules }: { modules: CurriculumModule[] }) { return <div className="relative space-y-3 before:absolute before:bottom-4 before:left-[19px] before:top-4 before:w-px before:bg-brand-100">{modules.map((module, index) => <details key={`${module.period}-${module.title}`} className="card group relative ml-0 overflow-hidden" open={index === 0}><summary className="flex cursor-pointer list-none items-center gap-4 p-4 sm:p-5"><span className="relative z-10 grid size-10 shrink-0 place-items-center rounded-full bg-brand-600 text-xs font-bold text-white">{String(index + 1).padStart(2, '0')}</span><span className="min-w-0 flex-1"><span className="block text-xs font-bold uppercase tracking-wider text-brand-600">{module.period}</span><span className="mt-1 block font-semibold text-navy-900">{module.title}</span></span><ChevronDown className="shrink-0 text-slate-400 transition group-open:rotate-180" size={19} /></summary><div className="border-t border-slate-100 px-5 py-4 pl-[4.75rem]"><ul className="flex flex-wrap gap-2">{module.topics.map((topic) => <li key={topic} className="badge">{topic}</li>)}</ul></div></details>)}</div>; }
